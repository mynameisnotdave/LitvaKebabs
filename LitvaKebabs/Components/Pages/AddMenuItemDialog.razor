<!-- Attr: https://www.c-sharpcorner.com/article/blazor-web-assembly-3-2-addeditdelete-fully-functional-application-part-2/ -->

@if (ShowDialog)
{
    <div class="modal fade show d-block" id="exampleModal" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="titleLabel">@if (AddOrModify == "add")
                        {
                            <span>Add</span>
                        } else if (AddOrModify == "modify")
                        {
                            <span>Modify</span>
                        }
                        menu item</h5>
                    <button type="button" class="close" @onclick="@Close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">
                    <EditForm Model="@MenuItem" OnValidSubmit="@HandleValidSubmit">
                        <DataAnnotationsValidator />
                        <ValidationSummary />
                        <div class="form-group">
                            <label for="lastName">Menu item name: </label>
                            <InputText id="menuItem" class="form-control" @bind-Value="@EditingMenuItem.Name" placeholder="Enter name of menu item"></InputText>
                            <ValidationMessage For="@(() => MenuItem.Name)" />
                        </div>
                        <div class="form-group">
                            <label for="firstName">Menu item price: </label>
                            <InputNumber id="menuPrice" class="form-control" @bind-Value="@EditingMenuItem.Price" placeholder="Enter menu item price"></InputNumber>
                            <ValidationMessage For="@(() => MenuItem.Price)" />
                        </div>
                        <button type="submit" class="btn btn-primary mt-2">Save menu item</button>
                        <a class="btn btn-outline-primary mt-2" @onclick="@Close">Close</a>
                    </EditForm>
                </div>
            </div>
        </div>
    </div>
}  