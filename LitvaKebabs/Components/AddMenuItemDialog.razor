@if (ShowDialog)
{
    <div class="modal fade show d-block" id="exampleModal" tabindex="-1" role="dialog">
        <div class="modal-dialog" role="document">
            <div class="modal-content">
                <div class="modal-header">
                    <h5 class="modal-title" id="titleLabel">Add menu item</h5>
                    <button type="button" class="close" @onclick="@Close" data-dismiss="modal" aria-label="Close">
                        <span aria-hidden="true">×</span>
                    </button>
                </div>
                <div class="modal-body">
                    <EditForm Model="@MenuItem" OnValidSubmit="@HandleValidSubmit">
                        <DataAnnotationsValidator />
                        <ValidationSummary />
                        <div class="form-group">
                            <label for="lastName">Menu item name: </label>
                            <InputText id="menuItem" class="form-control" @bind-Value="@MenuItem.Name" placeholder="Enter name of menu item"></InputText>
                            <ValidationMessage For="@(() => MenuItem.Name)" />
                        </div>
                        <div class="form-group">
                            <label for="firstName">Menu item price: </label>
                            <InputNumber id="menuPrice" class="form-control" @bind-Value="@MenuItem.Price" placeholder="Enter menu item price"></InputNumber>
                            <ValidationMessage For="@(() => MenuItem.Price)" />
                        </div>
                        <button type="submit" class="btn btn-primary">Save menu item</button>
                        <a class="btn btn-outline-primary" @onclick="@Close">Close</a>
                    </EditForm>
                </div>
            </div>
        </div>
    </div>
}  